# Rust
target/

# Web app build artifacts (kept at root so both main and public ignore them)
web/node_modules/
web/.next/
web/out/
web/tsconfig.tsbuildinfo

# Docs build output
docs/book/

# OS
.DS_Store
*.swp
*~

# Env
.env
.env.local

# Local agent / personal docs
.local/
CLAUDE.md
RELEASE.md

# Per-developer Claude Code tooling (skills, settings, task scratch).
# Skills here are local workflow helpers; if one is worth sharing with the
# team, check it in explicitly with `git add -f .claude/skills/<name>.md`.
.claude/
